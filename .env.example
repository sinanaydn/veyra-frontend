# Veyra RentACar — environment template
# Copy to .env.local and fill in values

# Spring Boot backend URL (server-side only, never exposed to browser)
BACKEND_URL=http://localhost:8080

# Public app metadata (safe to expose)
NEXT_PUBLIC_APP_NAME=Veyra RentACar
